# Environment

- **Python** — never use system Python. Use the project-local venv if one exists; if not, fall back to `/home/chavi/.venv/bin/{python,pip}` (don't create a new venv unless asked).
- **Node** — `npm install -g` needs no sudo (user-level prefix).
